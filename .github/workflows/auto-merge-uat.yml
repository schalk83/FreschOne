name: Auto Merge UAT to Main

on:
  push:
    branches:
      - UAT

jobs:
  merge-uat-to-main:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout UAT branch
        uses: actions/checkout@v3
        with:
          ref: UAT

      - name: Create Pull Request from UAT to Main
        id: cpr   # ✅ This is required for the output PR number
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: main
          branch: sync-uat-to-main-${{ github.run_id }}
          title: '🚀 Auto Merge: UAT → Main'
          body: 'Automatically syncing UAT into main.'
          commit-message: 'Auto-sync from UAT to main'
          delete-branch: true
          labels: automerge

      - name: Auto-merge PR
        uses: pascalgn/automerge-action@v0.15.3
        with:
          pull-request-number: ${{ steps.cpr.outputs.pull-request-number }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
